<div class="container">
  <div class="mt-3 mx-3">
    <h1>カテゴリー編集</h1>
      <h3 class="text-danger"><strong><%= flash[:danger] %></strong></h3>
        <div class="row">
          <div class="col">
            <div class="mt-3">
            <%= form_with model: [:admin, @category], local:true do |f| %>
              <h4>カテゴリー名</h4>
              <%= f.text_field :name %>　　　　　 <%= f.submit '変更を保存', class:"btn btn-success" %>
            <% end %>
            </div>
          </div>
        </div>
  </div>
</div>